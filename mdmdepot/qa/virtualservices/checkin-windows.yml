apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: default-checkin-windows
  namespace: gloo-system
spec:
  sslConfig:
    secretRef:
      name: mitsogo-tls
      namespace: default
  virtualHost:
    domains:
    - 'checkin-windows-qa.mitsogo.com'
    routes:
    - matchers:
      - regex: /.*
      routeAction:
        single:
          upstream:
            name: default-checkin-windows-4001
            namespace: gloo-system
---
apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: default-http-checkin-windows
  namespace: gloo-system
spec:
  virtualHost:
    domains:
    - 'checkin-windows-qa.mitsogo.com'
    routes:
    - matchers:
      - regex: /.*
      redirectAction:
        httpsRedirect: true
